# Changesets

Add a changeset for user-visible changes:

```sh
npm run changeset
```

Select `opencode-provider-orvix` and the appropriate semantic-version bump. Documentation, tests, and repository-maintenance-only changes do not require a changeset.
